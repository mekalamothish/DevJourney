/**
 * API configuration for DevJourney backend.
 * Centralized configuration for the ASP.NET Core /api/v1 endpoint.
 */

export const API_CONFIG = {
  baseUrl: 'https://localhost:7189/api/v1',
  defaultPageSize: 20,
  maxPageSize: 100,
};
